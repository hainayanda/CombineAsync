use_frameworks!

platform :ios, '13.0'

target 'CombineAsync_Example' do
  pod 'CombineAsync', :path => '../'

  target 'CombineAsync_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 7.0.0'
    pod 'Nimble', '~> 12.0.0'
  end
end
